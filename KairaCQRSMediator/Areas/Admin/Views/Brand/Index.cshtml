@model List<GetBrandQueryResult>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Layout/Index.cshtml";
}

@{
    int count = 0;
}
<div class="card">
    <div class="card-body">
        <div class="card-title">
            <div class="row">
                <div class="col">
                    <h4>Markalar</h4>
                </div>
                <div class="col text-end">
                    <a href="/Admin/Brand/CreateBrand" class="btn btn-primary ">Yeni Marka</a>
                </div>
            </div>
        </div>

        <table class="table table-bordered">

            <tr>
                <th>#</th>
                <th>Görsel</th>
                <th>İşlemler</th>
            </tr>
            @foreach (var item in Model)
            {
                count++;
                <tr>
                    <th>@count</th>
                    <td><img src="@item.ImageUrl" class="img-thumbnail" /></td>
                    <td>
                        <a href="/Admin/Brand/UpdateBrand/@item.BrandId" class="btn btn-warning">Güncelle</a>
                        <a href="/Admin/Brand/DeleteBrand/@item.BrandId" class="btn btn-danger">Sil</a>
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
